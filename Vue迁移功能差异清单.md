# 旧报告页面 → Vue 3 功能差异清单

本清单用于一次性梳理功能迁移，不是新版本路线图。功能先于视觉；旧 HTML 不再作为运行入口。研究口径和后端数据治理不因迁移而扩张，只有直接阻塞已存在页面功能的接口问题才纳入本轮。

## 比对基线与判定口径

- 旧页基线：统一 Vue 入口前一提交 `21a3efdd8a1cc5ae819bed233567117b302340d6` 的 `generate_level2_report.py`、`level2_workspace_ui.html`、`level2_state_ui.html`、`level2_kline_ui.html`、`level2_detail_ui.html`、`level2_patterns_ui.html`。可用 `git show <提交>:<文件>` 复核。归档 `level2_snapshots/*/report.html` 仅作为已冻结的历史证据，不在浏览器重新启用。
- Vue 基线：`9715789804d5e895f3529b34b164256b4674c6e8` 的 `frontend/src/App.vue` 及 `frontend/src/components/`；后续逐批更新本清单。旧页生成器中的早期错误文案和已经替换掉的“概率分”不是迁移目标，应以最终执行后的旧页为准。
- “已映射”仅证明当前源码存在对应展示/交互，不等于运行验收；“差异”表示旧页有而 Vue 缺失或语义不同；“待核”需要同一份报告或快照做交互对照；“新增”是 Vue 扩展，不要求倒退为旧实现。
- 旧页只覆盖复盘与形态；Vue 的“后续验证”是新增研究页签，按现有行为保留，不纳入旧页缺项。旧 HTML 快照的只读数据兼容与旧 UI 继续运行是两件事。

## 逐项功能映射

| 编号 | 旧页功能与来源 | Vue 对应位置 | 状态与需核事项 |
| --- | --- | --- | --- |
| A01 | 按本地日期列出报告、就绪/待更新/待计算；`workspace_ui` | `App.vue` 日期选择、`tradingDays.js` | 已映射；运行时核对缺来源、缺分钟和切换失败提示。 |
| A02 | 计算所选日期、轮询进度、完成后打开；`workspace_ui` | `App.vue` `buildSelectedDay` | 已映射；二次核对补齐同日重算后的组件身份重建，避免保留旧证据。 |
| A03 | 历史快照列表含日期、保存时间、图表数；`workspace_ui` | `App.vue` 快照选择 | 功能批 A 已改：选项列出保存时图表数；待统一浏览器验收。 |
| A04 | 快照直接显示保存时间、冻结范围和未保存图表边界；`workspace_ui` | `App.vue` 快照模式、各子组件 | 功能批 A 已改：显示 `savedAt`、冻结图数与未保存边界；早期快照仍需统一验收。 |
| A05 | 保存前拒绝图表、详情、连续状态或形态仍在加载；`workspace_ui` | `App.vue` `save` 与各组件状态 | 功能批 A 已改：组件登记在途证据，保存时拒绝并提示；含后续验证。待实际并发操作验收。 |
| A06 | 快照只读、切回最新、保存筛选/图表/详情/窗口/形态；`workspace_ui` | `App.vue`、`level2_workspace.py` | 已映射；旧快照缺字段保持未知，按实物逐项验收。 |
| A07 | URL 指定日期/快照；`workspace_ui` | `App.vue` `load` | 功能批 A 已改：显式 URL 不再等待日期/快照列表，先读取窗口元信息再加载目标；完整首屏耗时待统一浏览器验收。 |
| A08 | 旧报告 URL 仍可访问原页；`workspace_ui` | `level2_detail_server.py` 307 至 `/` | 按新要求改变：只保留 Vue UI，旧 HTTP 书签重定向；不重新提供旧界面。 |
| B01 | 7 日发布门槛、单位/方向口径、转换警告；主报告 | `QualitySummary.vue` | 已映射；缺失门槛明确未知，逐日警告需批内核对。 |
| B02 | 三表证券覆盖、时钟/日期/重复/关联键质量；`render_quality` | `QualitySummary.vue` | 已映射；质量字段只读呈现，不借迁移新增治理计算。 |
| B03 | 7 日市场成交额/净额/涨跌与量额误差 P95；主报告 | `App.vue` 概览、`MarketTimeline.vue` | 已映射；报告净额未知不得由连续共同样本替代。 |
| B04 | 十档末档、收盘时钟、主动净额非持仓变化的提醒；主报告 | `App.vue` 口径提醒 | 已映射；旧错误口径不得恢复。 |
| B05 | 已做证据、未知与交易边界；主报告 | `App.vue` 研究边界 | 已映射；正文是否在全部页签都可见待浏览器核对。 |
| C01 | 默认重点池、搜索转全库；主报告 | `CandidatePanel.vue` | 已映射；冻结名单与标签互斥口径不同，核对历史快照。 |
| C02 | 候选类型、上涨/下跌/平盘、资金/涨跌/成交额排序及正倒序；主报告 | `CandidatePanel.vue`、`candidateFilters.js` | 已映射；同值/未知排序及跨筛选组合做局部测试。 |
| C03 | 卡片名称代码、标签、价格方向、收盘/VWAP、涨跌、净额、未知方向金额、十档；主报告 | `CandidatePanel.vue` | 已映射；缺数不能显示为 0，运行核对样本。 |
| C04 | 点击卡片查看模态详情，关闭/Esc/背景退出；主报告 | `CandidatePanel.vue` 原生 `dialog` | 已映射；图表叠层已修，仍需批内键盘焦点验收。 |
| C05 | 详情中收盘/盘口时钟、五时段覆盖、量额对账；主报告 | `CandidatePanel.vue` | 已映射；按已保存证据显示，盘后时点不称可交易。 |
| C06 | 五时段主动成交、关联编号金额组、`order_raw` 类型/方向；主报告 | `DetailEvidence.vue` | 已映射；旧摘要与新式深查不能混称，历史快照逐项核。 |
| C07 | 非重点股点击计算、缓存/轮询/重试；`detail_ui` | `DetailEvidence.vue` | 已映射；局部测一只未预计算股票和错误路径。 |
| C08 | 历史标签、当日涨跌/净额、后 1/3 日原始收盘变化；主报告 | `CandidatePanel.vue` 详情表 | 已映射；未复权与未观察不可误写成收益 0。 |
| C09 | 已应用连续状态补入候选卡/详情；`state_ui` | `CandidatePanel.vue` | 已映射；同日重载报告也按加载代次重建候选、详情与图表组件。 |
| D01 | 前/后一交易日、逐日 ±1、3/5/20 快捷、手动应用；`state_ui` | `ContinuousPanel.vue` | 功能批 A 已改：元信息决定默认/上限，`?window=` 恢复；逐日输入与快捷上限校验已加。 |
| D02 | 切换交易日保留待应用窗口；`state_ui` `navigate` | `App.vue` `moveTradingDay` | 功能批 A 已改：待应用窗口保留到换日，实时 URL 同步；待实际换日验收。 |
| D03 | 请求序号防迟到覆盖，失败保留旧窗口，快照窗口只读；`state_ui` | `ContinuousPanel.vue` | 二次核对发现调整待应用窗口未取消旧请求；已补窗口输入/±1/快捷的请求失效与轮询清理，旧服务任务可继续但不能覆盖新选择。 |
| D04 | 共同方向样本逐日成交额、加权净额比、净买入占比、覆盖；`state_ui` | `ContinuousPanel.vue` | 已映射且扩展价格宽度；共同样本与全市场分母不混用。 |
| D05 | 最新日相对前日改善/恶化/持平/未知及严格跨零转向；`state_ui` | `ContinuousPanel.vue` | 已映射；一日窗口显示不适用。 |
| D06 | 个股名称代码搜索、变化筛选、20 只分页；`state_ui` | `ContinuousPanel.vue` | 已映射；筛选结果/页码复位做局部测试。 |
| D07 | 组合预设、方向转换、金额/净额比/价格范围和持续次数高级筛选；`state_ui` | `ContinuousPanel.vue`、`continuousFilters.js` | 已映射；非法上下限和未知值不补 0。 |
| D08 | 个股区间价格、净额比、日变化、窗口加权、方向/成交额覆盖及逐日证据；`state_ui` | `ContinuousPanel.vue` | 已映射，且新增回撤/相对基准/组内比较；逐日来源与价格单独覆盖。 |
| D09 | 每张卡确定性文字，例如“仍为净卖出但较昨日改善”；`state_ui` `narrative` | `ContinuousPanel.vue` 卡片 | 功能批 B 已改：逐股确定性资金解释；缺证据、一日窗口保持未知/不适用。 |
| D10 | 同向、连续改善和连续恶化的可观察次数/左截断；`state_ui` `transitions` | `ContinuousPanel.vue` | 功能批 B 已改：改善、恶化均直显，可观察左截断写“至少”。 |
| D11 | 详细方法/来源及窗口覆盖说明；`state_ui` | `ContinuousPanel.vue` 逐日来源、脚注 | 功能批 B 已改：方向完整/不足数量、详细方法及已保存来源对象入口；旧快照未存来源时明确未知，不补读当前文件。 |
| D12 | 旧快照缺连续正文不从当前数据回填；`state_ui` | `ContinuousPanel.vue`、`level2_workspace.py` | 已映射；用早期 HTML 快照验收。 |
| E01 | 代码悬浮日 K、“分”悬浮当日分钟 K、点击固定、移开关闭；`kline_ui` | `KlineTrigger.vue` | 已映射；当前本地源每次重新读取，快照只读冻结。 |
| E02 | 日 K 前复权、分钟昨收 0 轴、缺档灰带、成交量；`kline_ui` | `KlineTrigger.vue`、`chartModel.js` | 已映射；分钟 0 轴垂直居中是后续明确要求，不能退回旧轴。 |
| E03 | 悬浮/左右键逐根 OHLC、量、额；`kline_ui` | `KlineTrigger.vue` | 功能批 C 已改：分钟逐根读数增加相对昨收涨跌幅；昨收缺失不补值，微小变化用更高精度。 |
| E04 | 零成交分钟、异常 OHLC、读取时间和来源口径；`kline_ui` | `KlineTrigger.vue` 质量折叠区 | 功能批 C 已改：零成交分钟计数直显，缺字段仍为“未保存”。20260922 的 000977.SZ 分钟接口含 240 根、零量 2 根。 |
| E05 | 日/分钟图在候选与连续卡、详情中可用；`kline_ui` | `KlineTrigger.vue` 各入口 | 已映射；模态内顶层预览已单独修正，批内再核对全入口。 |
| F01 | 复盘/形态两页签、左右方向键切换；`patterns_ui` | `App.vue` 三页签 | 功能批 C 已改：左右/Home/End 键与 roving `tabIndex`；第三页签“后续验证”为新增。 |
| F02 | 形态计算/刷新、进度和失败提示；`patterns_ui` | `PatternPanel.vue` | 已映射；旧结果与失败新请求不能混标。 |
| F03 | 搜索、阶段、方向、可评估性、形态多选与任一/全部共存；`patterns_ui` | `PatternPanel.vue`、`patternFilters.js` | 已映射；按同一冻结结果局部测组合。 |
| F04 | 时效、量能、突破距离、MA20、MACD、当前 L2 资金高级筛选；`patterns_ui` | `PatternPanel.vue` | 已映射；L2 必须只用同日报告已应用窗口。 |
| F05 | 排序、20 只分页、无结果/无效条件、规则参数；`patterns_ui` | `PatternPanel.vue` | 已映射；边界局部测。 |
| F06 | 事件抽屉切事件/副图/MA/标注，逐根图和键盘左右键；`patterns_ui` | `PatternDetail.vue` | 功能批 C 已改：打开聚焦关闭按钮，Tab 循环、Esc 关闭、退出恢复原焦点；实际键盘验收待总验收。 |
| F07 | 形态结果与已打开逐股图随快照冻结，缺图不补读；`patterns_ui` | `PatternPanel.vue`、`PatternDetail.vue` | 已映射；早期快照与新快照各测一次。 |
| G01 | 旧报告 HTML 页面作为服务端模板运行；主报告及五脚本 | `level2_detail_server.py` 仅服务 Vue `dist` | 按用户要求移除：不恢复旧 HTML UI。旧文件/快照如属历史证据，保留只读数据而非页面入口。 |
| G02 | 旧书签/快照链接可找回对应证据 | 旧 URL 307、`snapshot_document` | 已映射；需核对日期与快照参数不丢失，不让旧 `file://` 冒充 HTTP 入口。 |

## 批次与验收顺序

1. **功能批 A：入口、日期、快照和连续窗口上下文。** 处理 A03–A07、D01–D03；优先解决显式 URL 首屏被列表阻塞、窗口默认/上限/换日保持、快照身份和保存期间的明确状态。仅跑相关前端测试与单条页面路径验收；不运行全量测试或提交单点改动。
2. **功能批 B：连续证据、候选与详情。** 处理 D09–D11，连同 C01–C09 的联动、历史快照、缺数/一日窗口用例集中核对；需要时只改展示层和已有接口字段，不启动新的数据治理项目。跑对应筛选/详情局部测试。
3. **功能批 C：图表与形态交互。** 处理 E03–E05、F01/F06，复核 F02–F07；含分钟逐根涨跌、来源质量、键盘/模态焦点与快照缺图。跑图表/形态局部测试及少量浏览器路径。
4. **功能迁移总验收。** 基于本清单逐项更新“完成/未完成/证据”，同日实时报告和早期/新式快照都要覆盖；全部功能批完成后才统一执行完整 `pytest`、`npm test`、`npm run build` 与浏览器验收。此前每批只做局部测试。完整测试和页面验收通过后再统一提交/推送；不自动推送未验收改动。
5. **视觉批：仅在功能一致性之后。** 集中比对主报告与连续区的卡片列数、字号、密度、配色和断点；不在功能批中穿插单点 CSS 改动。视觉以可获得的旧源码/已保存截图为参照，不宣称逐像素相同。

功能批 A 局部验证：`node --test test/tradingDays.test.js test/evidenceBoundary.test.js` 8/8 通过；七个涉及的 Vue 单文件组件通过模板语法编译检查。运行时并发保存、切换与快照场景仍放在总验收，不提前宣称完成。

功能批 B 局部验证：连续、候选、详情四组定向测试 30/30 通过；`ContinuousPanel.vue` 模板编译检查通过。20260922 的 3 日接口实物为 5209 只股票、方向完整 5204 只，覆盖统计与页面派生口径一致。C01–C09 的跨面板实际操作和旧快照仍待总验收。

功能批 C 局部验证：图表/形态三组定向测试 8/8 通过；`App.vue`、`KlineTrigger.vue`、`PatternDetail.vue` 模板编译检查通过。旧快照缺图、完整交互路径与焦点恢复待统一浏览器验收。

## 本轮统一验收记录

- 完整测试：`pytest -q` 149/149、`npm test` 50/50；`npm run build` 成功。视觉批只调整宽屏候选网格后再次构建成功；未对视觉改动重复运行完整测试。
- 浏览器：20260922 实时报告在显式 URL 下先加载目标报告、日期与快照列表随后出现；`?window=3` 恢复已计算三日结果，逐股解释、改善/恶化次数与 5204/5209 覆盖一致。搜索 `301080` 仅剩百普赛斯，详情引用同一已应用窗口，Esc 关闭后保留筛选。
- 快照：20260925 的无图快照显示保存时间和冻结范围，缺图明确拒绝读取最新文件；20260923 最早快照缺质量报告时保持“未保存”，但已冻结的 `minute/000977.SZ` 可读且新增分钟涨跌幅不改变其冻结 OHLC。无效快照给出读取失败和重试入口。
- 并发保存：形态全市场任务运行期间点击保存，明确提示 1 项证据在途并拒绝冻结。形态扫描完成后在浏览器打开潍柴动力事件，逐根图可用；反向 Tab 留在弹窗，Esc 关闭后焦点回到原事件按钮。
- 视觉批：桌面 1280 宽的候选网格恢复四列；原 597 宽的两列仍保持。保留 Vue 的独立连续证据和研究页签，不强行还原旧页面的内联脚本布局；无逐像素一致性主张。
- 未覆盖的实际路径：另一报告日期目前无“ready”结果，无法在不额外计算原始报告的条件下验收成功换日后的窗口延续；代码与定向测试已覆盖参数和组件重建口径。浏览器仅覆盖一个在途保存案例，所有并发交错不是形式化证明。上述两项保留为后续有对应本地结果时的运行验收点，不扩大后端治理任务。

验收还须检查：切换失败保留原证据身份；快照绝不补读当下原始数据；未知不补零；同一数值不能因排序/筛选改变口径；旧页显示的研究标签不升级为吸筹、买卖或支撑确认。

## 多角度二次差异核对（2026-09-26）

- 功能核对：旧页 `pending()` 在窗口输入、±1 和快捷按钮上会取消旧请求的展示资格；Vue 原先仅改输入。已将失效序号、清轮询和解除旧请求的界面等待统一放进 `stageWindow()`。旧页换报告会整页重建，Vue 原先对同日实时报告复用 `mode:day` 组件键；已改为每次成功加载增加证据代次，重建候选、连续、形态、后续验证组件。两项属于旧功能迁移状态差异，不涉及后端数据治理。
- 运行核对：5173 开发入口与 18762 服务入口的同日页面、代理 API 和静态资源一致，浏览器无错误/警告。`?window=5` 只预选窗口；当 `/api/state/view` 返回 `missing` 时显示“尚未计算”，不是样式或缓存损坏，仍须手动应用。此项保留现有“不自动扫描原始 Level-2”的边界。
- 集中视觉批：压缩 hero、工具栏、指标及候选卡的空白；逐日轨迹由自动五列改为桌面最多三列、窄屏递减。候选证据字段、连续逐日来源与覆盖不删减。连续个股维持三列，因其新增回撤、相对基准与组内比较，不强行挤回旧页四列；这属于有依据的版式差异，不宣称逐像素一致。
- 验收状态：定向测试 12/12、Vue 组件编译、完整 `pytest -q` 149/149、`npm test` 52/52 和 `npm run build` 均通过。5173 开发页与 18762 新构建生产页分别实际打开：20260922 五日状态 5202/5209，只在服务端已有完成结果后显示；生产页候选四列、逐日轨迹三列，hero 高 133px；596px 下候选保持双列且浏览器无错误。新构建中切换到既存快照后 URL、只读标识和未保存图表边界正确。新增的请求代次单元测试覆盖旧响应失效，但浏览器未人为注入迟到响应；同日原始报告重算成本较高，本轮未触发真实重算，因此两条并发链路仍保留为有条件的运行验收点，不宣称已实测所有交错。
