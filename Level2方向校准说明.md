# Level-2 方向校准

适用来源：`/Volumes/990pro/data/level2` 的英文逐日成交结构；价格单位为分。不是全供应商通用约定，也不涵盖尚未接入的按月个股归档。

## 当前映射

- `Side=0`：主动买入；关联 `BuyID`；净额贡献正金额。
- `Side=1`：主动卖出；关联 `SellID`；净额贡献负金额。
- `Side=-1/-11`：撤单，排除成交统计。
- 逐条检查：买卖编号均为正且不等，0对应 `BuyID>SellID`，1对应 `SellID>BuyID`。未知码、缺编号、相等或冲突保留未知金额；不翻转映射迁就数据。
- 包含未知金额的股票日不输出可用的方向状态，避免将部分识别净额冒充完整净额。

## 校准证据

数据自带 `level2数据说明.txt` 第25行定义0买1卖。旧技能 `analyze-a-stock-level2` 及 `stock_level2_summary.py` 曾反写为1买0卖，现已按来源限定修正。原始Parquet未修改。

使用 `calibrate_level2_direction.py` 于2026-09-23重新读取原始文件，过滤同日、正价格/数量、A股代码，排除撤单，按沪深分别核对；不将全市场逐笔加载为内存DataFrame。

| 交易日 | 沪市有效成交 | 深市有效成交 | 两市编号方向一致率 |
|---|---:|---:|---:|
| 20260331 | 61,090,324 | 79,232,271 | 均100% |
| 20260630 | 74,862,984 | 98,108,777 | 均100% |
| 20260827 | 63,741,685 | 80,951,184 | 均100% |
| 20260831 | 67,197,354 | 82,885,957 | 均100% |

合计608,070,536条，有效编号覆盖100%。编号检查与Side可能共享供应方生成逻辑，因此不把两者视为完全独立证据。

独立盘口交叉检查：四日的000001、000977、600519、688008；限连续竞价，使用严格早于成交、相差不超过1秒且买一低于卖一的前序快照，仅保留成交价触及/越过买一或卖一的可判断样本。719,051条符合筛选，其中611,823条（85.09%）支持0买1卖，反向为14.91%。快照离散、期间行情变化与时钟差异导致其不是逐笔真值；它只作为交叉支持，不用于强行改写个别方向。

可复核原始计数及文件大小/修改时间保存于本机 `.level2_direction_audit.json`；执行 `python calibrate_level2_direction.py` 可重跑。统计指纹不是原文件全量哈希，不构成防篡改证明。

## 生效与历史边界

连续状态缓存含计算代码指纹，修复后旧缓存不再命中，读取时按新口径重新聚合。已保存快照保持原样，不自动覆盖；需要重新计算并保存新快照以获得校准结果。

当前中文原生数据继续使用B/S映射，未改为数字规则。校准不会补出缺失交易日，不会把历史复盘变成当时可得数据，也不是吸筹或交易有效性验证。

运行验收：截止20260922的20日窗口已重新计算并在页面核验。浪潮信息方向覆盖由15/20恢复为19/20，仍缺20260902，窗口加权资金水平及趋势不会冒充完整。其20260831主动净额为+255,999,740.34元，未知方向金额0；独立旧技能助手重算与连续状态服务一致。34项测试通过，原报告5209只股票、7日基准校验通过。

旧技能生成的其他历史独立报告未批量重写，须逐份重算后才能恢复其方向结论；不能仅把展示净额乘以负一替代完整关联单重算。
