# Level-2 数据实战使用框架

## 0. 文档设计

这是一份单一、连续、持续维护的研究规范。正文始终表达当前有效设计，不拆分为 `v1`、`v2` 或运行批次。

- 规则变化直接修改对应章节；
- 新内容归入既有主题，不重复建立一套定义；
- 交易日只标识证据日期，不标识文档版本；
- 交易日报告是规范在某个日期的执行结果，不替代规范本身；
- 当前正文不保留旧规则、差异说明或审计章节。

Level-2 用于验证价格行为、流动性和成交结构，不能单独证明主力身份、吸筹意图或未来收益。

## 1. 数据边界与质量门槛

默认 Level-2 根目录：

```text
/Volumes/Sn850X_4T/data/level2
```

常用文件：

```text
deal_YYYYMMDD.parquet
snapshot_YYYYMMDD.parquet
order_raw_YYYYMMDD.parquet
```

- `deal`：逐笔成交、主动买卖和成交冲击；
- `snapshot`：盘口、十档深度、价格和日内状态；
- `order_raw`：新增委托、撤单、队列和补单研究。

每日分析前检查：

- 正式文件是否存在；
- 是否存在 `.partial`、失败目录或未提交文件；
- 三类文件的证券覆盖是否一致；
- 时间戳是否连续、是否重复或乱序；
- 价格、数量、金额单位是否确认；
- `BS` 方向未知比例；
- 快照与逐笔成交的成交量、成交额能否基本对齐。

数据不完整时使用 `UNKNOWN` 或 `INCOMPLETE`，不能把缺失当作零，也不能把临时文件当正式数据。全市场扫描默认只纳入沪深A股，排除 ETF、可转债和其他非股票品种。

当前已实现目标日三表覆盖交并集、缺少证券清单、日期异常、非法时钟/零时钟/盘后记录、有效成交未知方向金额，以及成交候选键重复与两种委托编号匹配核验；以单独质量模块显示在原报告中，支持来源指纹缓存与快照冻结。分母、源文件、未完成检查可展开查看，复核入口为 `Level2数据质量核验.ipynb`。不是三表完全一致或数据无误的认证。

原报告、按需深查与连续状态统一方向降级，保留已识别净额与未知金额，不用0冒充完整净额。关联编号按日期/股票/方向分组，无效编号单列且不进入大单档；原生源缺频道，候选字段匹配不能单独证明经济订单身份。重复键不自动去重，撤补单、时间乱序及逐字段完全重复行仍待核验。

20260922实际核验：三表均覆盖5209只A股；有效成交未知方向金额0；成交候选键14组重复、涉及28行。进一步读取这28行后确认，它们均是09:25记录与15:00后记录编号重用，字段完全重复行数为0（仅指这些候选行，不扩展为全库完全重复检查）。例如600272.SH的成交编号9388对应09:25价格16.33元/500股及15:06:05.670价格17.35元/300股，不能删除任意一条来“消除重复”。这说明股票/日期/成交编号尚不足以作为唯一经济事件键。

当日两种原始委托字段的候选匹配比例分别为0.2562%和61.2312%；后者有877146条成交对应重复委托键，不能由匹配率直接确认订单身份。order_raw零时钟2313行单列为待解释状态；不据此声称2313条错误委托。上述数值仅针对该交易日，其他日期必须重新计算。

## 2. 连续状态模型

市场和个股不是“好/坏”的二元状态。建议同时跟踪：

```text
市场强弱、资金主动性、上涨扩散度、流动性、波动率、盘口承接能力、高位兑现压力
```

每个变量同时保存：

- 当前水平 `Level`；
- 相对前一时段或前一日的变化 `Slope`；
- 状态持续时间 `Persistence`。

盘中比较09:30—10:00、10:00—10:30、10:30—11:30、13:00—14:00和14:00—收盘。日级状态逐交易日更新，不等待第3、5或20天才作判断。3／5／20交易日只是可配置的初始观察窗口，不是已验证的最优参数；观察窗口、信号有效期和持有期分别定义。

个股状态可以连续迁移：

```text
弱势流出 → 卖压衰减 → 被动承接候选 → 主动承接 → 结构修复
→ 主动推动 → 高位分歧 → 派发确认 → 支撑失效
```

每个股票/交易日形成状态向量：

```yaml
price_state:
active_flow_state:
large_order_state:
book_state:
impact_state:
liquidity_state:
volatility_state:
support_state:
distribution_state:
```

### 2.1 连续状态的计算约定

当前已实现日级净额比水平／变化／加权窗口水平／斜率／持续性、成交额及窗口复权价格变化，并支持逐交易日调整窗口。以下约定仍包含尚未实现的相对收益、回撤、盘口连续状态及验证能力，不能将整节视为全部完成。

- 水平、变化、持续性分别保留，不合并成未经校准的综合分或吸筹概率。
- 主动净额比当日水平为 `net / amount`；窗口水平为 `sum(net) / sum(amount)`，不能简单平均每日比例。分母无效时为未知。
- 净额比日变化以百分点表示。窗口趋势可采用每日净额比对交易日位置的线性斜率，单位为百分点／交易日；至少两个有效点且窗口完整才输出正式趋势。斜率只描述变化，不代表预测效果。
- 持续性分别记录连续净买入／净卖出交易日数、连续改善／恶化次数、窗口同向占比。三天观测最多支持两次相邻改善；连续改善不等于连续买入。方向按未四舍五入数值判断，零值单列；若使用噪声容差须记录参数且不得宣称已验证。
- 历史起点之前未知时显示“至少N日／次”，不假设持续状态恰好从可见起点开始。缺失切断可证明的连续链，不能越过缺口拼接；已确认停牌单独展示，不当作零资金流或正常连续证据。
- 价格组保留当日与窗口复权收益、窗口回撤、相对基准收益；基准名称、复权锚点和数据来源可查看。基准缺失时只禁用相对收益，不将其填零。
- 成交组先展示成交额水平及变化。相对活跃度必须明确分母使用的历史区间；历史不足不生成分位。盘口组只将目前可得的连续竞价末档十档不平衡称为“末档观测”，不称为全日承接能力。
- 主动方向未知导致净额方向不稳健时，方向持续性和资金候选标签降级；时间异常、缺盘口等质量问题按受影响指标处理，不能一律用零补齐。
- 状态允许并存、停留和反转，不强制按上述示意链单向演进。正文依次表达事实、变化、反证和未确认条件；卖压衰减不得自动升级为吸筹。

### 2.2 数据与计算职责

将日级基础聚合、连续特征计算、页面展示分开。按来源与股票／交易日保留价格、成交额、主动净额、未知方向金额、有效性和覆盖信息；历史特征只用截至该日可得的观测。当前页面 `history` 只有标签、涨跌幅、净额和后续原始收益，不足以重算历史净额比、活跃度或盘口变化，必须扩充服务端日级数据，不能由前端猜测。

页面展示范围默认7日，与特征窗口独立；查看20日状态不要求页面铺开20天表格。特征结果记录所需交易日数、有效数、缺失日期、参数和来源标识；20日不足仅禁用该特征，不阻断有效的短窗指标。交易日历不能仅由现有数据文件推定，需防止日历文件本身缺日造成假连续。

历史价格与未来结果分开存储和展示；后1／3／5日结果不能参与当时状态或候选排序。市场聚合还需显示每天样本数与共同样本变化，避免把上市、停牌或缺失引起的成分变化误认为市场改善。

## 3. 全市场横截面扫描

全市场扫描负责描述市场状态和生成候选池，不直接授权下单。

### 3.1 市场指标

每日计算成交额、主动净额、上涨/下跌/平盘家数、上涨占比、上涨和下跌股票中的资金分布、成交额分位、价差、盘口深度和波动率。

建议输出：

```yaml
MarketStrength:
BreadthScore:
ActiveFlowScore:
LiquidityScore:
VolatilityScore:
DistributionRisk:
```

全市场主动净额表示主动成交方向，不等于资金离开市场。

### 3.2 个股横截面特征

每只股票记录日涨跌幅、成交额、VWAP、主动净额、主动净额/成交额、大额父单净额、买卖深度不平衡、日内高低点、收盘位置、相对市场收益、数据覆盖和异常标记。

横截面排序优先使用相对成交额、历史分位和流动性分层排名。固定“100万元大单”只能作辅助，不能跨股票直接比较。

### 3.3 四象限候选

| 价格 | 主动资金 | 候选标签 |
|---|---|---|
| 上涨 | 净流入 | 主动推动候选 |
| 下跌有限 | 净流入 | 主动承接候选 |
| 上涨 | 净流出 | 高位分歧/被动承接候选 |
| 下跌 | 净流出 | 弱势回避候选 |

四象限是描述工具，不是交易指令。

## 4. 选股与买点

### 4.1 选股

优先寻找：

```text
相对市场更强 + 资金状态边际改善 + 成交可执行
+ 价格仍处于合理位置 + 关键支撑抬高 + 价格冲击低于历史基准
```

“主动流入榜首”只能生成候选，不能直接买入。

### 4.2 买点确认

只在预先定义的支撑、突破或回踩位置观察：价格守位或收回，主动卖压递减或大额父单连续净买入，买方队列补回，盘口恢复改善，价差和容量可接受。

买点触发需要至少两个或三个独立维度同向。失效包括放量破位、买方被击穿后不恢复和大额父单转负。

## 5. 卖点与风险退出

### 5.1 减仓预警

```text
价格创新高 + 主动大单净流出 + 卖方深度扩大
+ 买方补单变慢 + 冲高回落或收盘位置变差
```

高位主动流出是风险预警，不能因为一个指标清仓。

### 5.2 硬失效

关键支撑被放量击穿、大额父单继续净卖出、买方盘口无法恢复、价格冲击扩大或退出容量恶化时，优先执行风险退出。

## 6. 承接、吸筹、换手与派发

### 6.1 被动承接候选

需要同时观察主动卖压、实际价格冲击、买方补单、关键价位恢复和后续结果。初步标签：

```yaml
PassiveAbsorptionCandidate: true
PassiveAccumulationConfirmed: false
```

“主动净流出但价格上涨”也可能是高位派发或正常换手。连续证据和后续1、3、5日结果只更新后续时点的研究判断，不回填信号日状态；在概率模型经过校准前只描述证据与反证，不输出吸筹概率。

### 6.2 主动承接

```text
价格下跌有限 + 主动净额为正 + 大额父单未转负
+ 日内低点或关键支撑未失守
```

需要观察次日资金、成交额和信号日低点。

### 6.3 派发候选

```text
高位放量 + 主动大单持续净卖出 + 卖方深度偏厚
+ 买方补单失败 + 冲高回落 + 后续支撑失效
```

“上涨+主动净流出”只是预警，后续1—3日支撑失效或盘口恢复失败后，才可升级为派发确认。

## 7. Level-2微观结构特征

- 逐笔成交：主动买卖、成交笔数、单笔金额、连续同向成交、成交脉冲和成交后的价格反应；
- 父单：父单净额、拆单结构和相对成交额，结合历史分位使用；
- 订单簿：距离加权深度、Microprice、价差、深度衰减、补单速度和恢复时间；
- 撤单：结合新增委托、成交和补单解释，区分主动撤单、成交减少和被动过期；
- 价格冲击：`中间价变化 / 有符号成交量`，并区分短期冲击和永久冲击。

成交证据权重高于静态挂单。固定阈值、单一大单和单次十档快照不能单独证明意图。

## 8. 多时间尺度与信号半衰期

盘中5／15／30分钟、日线3／5／20交易日均为可配置初始观察尺度。状态逐次更新，固定窗口只是比较视角，不是交易触发时间点。重叠窗口共享证据，不能将三个窗口同向当作三个独立确认。阶段层观察上涨初期、主升、高位分歧和下跌修复，但不凭标签确认阶段。

每个信号测量5秒、30秒、5分钟、30分钟、次日、3日和5日的有效性。几十秒内有效的信号属于执行型信号，能影响未来1—3日的信号才适合纳入日线模型。

有效期与窗口选择属于待验证的研究结果。只有时间分辨率、覆盖和执行约束支持的期限才可测试；按时间划分训练与样本外区间，控制重叠样本、参数尝试与成本，不能在同一段历史选参数再宣称验证成功。

事件观察作为固定窗口的补充：每个事件记录发生时间、最早可识别时间、触发规则、使用的数据、失效条件及状态。若需要收盘或后续确认，事件不得回填为更早已知。盘中数据尚不能支持的守位、补单事件保持未实现；人工选择的起点标记为研究注释，不能进入自动信号绩效统计。参数改变形成当前观察设置，不覆盖此前冻结的快照。

当前已实现的日级事件只使用应用窗口内相邻且方向有效的两个交易日：净额比仍负而收敛、仍负而扩大、严格负转正、严格正转负。零值不冒充转向；缺失日中断连续性。触发日只记录该日日级数据就绪后可识别的事实，后续观测单独将事件标为仍满足延续条件、已失效、已结束或连续性未知，不回写触发日。负值收敛只表示`净额／成交额`的比例变化，不证明绝对卖出额下降、盘口补单或吸筹。单日窗口无窗口内相邻日，不触发事件；旧快照缺此层时不以新数据补齐。

## 9. 市场和行业相对化

个股信号结合相对市场收益、行业残差收益、市场/行业中性主动净额、同市值和同流动性分层排名。市场普遍下跌时，主动净流入可能表示相对强势；市场普遍上涨时，主动净流入很小的股票可能反而弱于市场。

## 10. 验证与执行

每个候选记录：

```yaml
evidence_date:
price_state:
active_flow:
large_parent_flow:
book_state:
impact_state:
candidate_label:
trigger:
invalidation:
next_1d_return:
next_3d_return:
next_5d_return:
max_drawdown:
fill_rate:
slippage:
```

回测包含手续费、滑点、涨跌停、成交容量、延迟和无法成交情况。研究信号有效不等于实盘可执行。

状态使用 `DRAFT`、`RESEARCH`、`PAPER_ONLY`、`VALIDATED`、`PRODUCTION`、`RETIRED` 和 `INVALID`。状态升级依赖验收条件，不依赖执行次数。

## 11. 全市场日复盘输出

```text
一、数据覆盖与质量
二、市场连续状态
三、主动资金推动候选
四、下跌中的主动承接
五、被动承接候选
六、高位分歧/派发风险
七、重点个股深度复核
八、未来1/3/5日验证计划
九、数据不足或不可交易名单
```

报告文件使用规范名称和证据日期，例如：

```text
level2-market-scan_20260922.md
```

同一日期重复执行时更新该日期结果，不创建新的版本、批次或差异报告。生成时间可以保存在技术字段中，但不进入报告版本或主要结论。

### 11.1 连续状态在现有页面中的设计与实现边界

已接入连续观察控件、卡片与详情日级证据、前后交易日导航及已加载窗口的快照冻结；已增加共同样本逐日统计、结构变化、独立证据卡片及组合／高级筛选。日级事件层已加入四类严格相邻日净额比变化与后续状态；它不包含盘中守位、补单或交易触发，旧快照缺失事件层时不补读。独立命令行事件后续收益研究已接入收盘后1／3／5交易日的描述统计、训练／隔离／样本外切分和成熟度标记；尚未进入页面、快照或可交易回测。连续指标专属排序尚未接入。历史逐日成交文件可从补充目录读取；按月个股归档尚未接入。已校准的990pro逐日英文旧来源使用0买1卖，并逐条核验编号；未知来源、缺编号和冲突方向仍为未知，详见 `Level2方向校准说明.md`。

旧报告保留深色样式、日期／快照管理、候选筛选与排序、代码日K／“分”分钟图及详情弹窗。现正以同仓库的 Vue 3 页面逐项迁移交互，Python API 统一提供事实与计算；迁移期间旧页与新页并存，不把下表设计目标误写成已经全部实现。页面布局目标如下：

| 现有位置 | 增加内容 | 使用边界 |
|---|---|---|
| 日期／快照区 | 简洁的当前报告日、数据截止与冻结状态 | 报告日、读取时间和保存时间不混为实时行情 |
| 市场轨迹下、候选列表前 | “连续观察”设置及市场变化摘要；回看长度支持按1个交易日增减或直接输入，3／5／20仅为快捷选项；显示起止日期和所需／有效日数 | 展示范围与计算窗口分离；设置变更需应用计算，未完成仍显示旧参数并提示 |
| 候选卡片 | 在现有价格、资金和十档下增加净额比日变化、方向持续性与改善持续性；必要时显示质量提示 | 不在卡片堆叠全部窗口，不添加无依据的“吸筹确认”徽标 |
| 筛选排序栏 | 增加改善／恶化、连续性可判定／未知，以及明确单位的变化指标排序 | 未知单独保留，正序和倒序均不将未知视为零；切换窗口同步注明排序口径 |
| 个股详情顶部 | 简短结论和“指标—水平—日变化—窗口趋势—持续性—质量”表，接逐日证据轨迹 | 同时列反证；原始成交、关联单和委托统计保留在下方，不用结论替代证据 |
| 个股详情事件观察 | 已定义事件、起点、可识别时间、持续证据、失效与未知条件 | 只有已有明确规则和数据的事件可计算；人工锚点单独标识 |
| 个股详情后续验证 | 独立显示后1／3／5日结果和窗口是否成熟 | 不参与前述历史状态；未到期、缺失、未验证分开显示 |

桌面端详情使用表格，窄屏按指标纵向展开，默认只显示当前选中的比较窗口；其余窗口折叠查看。保留当前搜索、筛选、排序和详情滚动状态；点击卡片或窗口变化不得误触行情悬浮图。

卡片最多新增两行核心证据，例如“净额比及较昨日变化”“净卖出持续日数及改善次数”；这些数字需来自正式计算，不使用示意值填充。市场原表可增加净额比及日变化列。趋势图如使用价格与资金两组数值，采用共享日期轴的分开图，不用双纵轴暗示相关性；缺失日期断线。未知排序值始终放最后，以股票代码稳定处理并列。

观察参数放在独立研究配置中，不混入只负责数据路径的 `level2_paths.json`。本地读取模式分别展示报告计算时间与图表读取时间，悬浮更新图表不代表连续状态已重新计算。

逐日调整分为两种独立操作：

- **移动观察截止日**：提供“前一交易日／后一交易日”，按可核验交易日历移动，不按自然日移动，也不跳过缺数据的交易日。截止日默认跟随报告日期；改变截止日时整份报告、行情图、资金统计和连续状态同步切换。目标日尚未就绪时提示待计算或数据不足，不将旧日期结果显示为新日期结果。
- **调整回看长度**：截止日不变，使用“−1交易日／+1交易日”或直接输入正整数，例如由近5日调整为近6日、近7日。窗口包含截止日；3／5／20只是快捷填入值，不限制其他长度。单日窗口可显示水平，但窗口内趋势需至少两个有效点；较前日变化须另有前一交易日有效观测，不能因为窗口为1日就填零。
- **覆盖与计算反馈**：每次显示实际起止交易日、所需日数、有效日数及具体缺失日期。不能跳过缺口凑足N日，也不能将历史不足自动缩窗后仍标原长度。应用参数时显示计算中，完成后统一更新结果和排序口径。
- **快照边界**：只允许查看快照中已冻结的截止日与窗口结果组合；即使基础数据似乎足够，也不在只读快照内临时推导新窗口。未保存组合显示“快照中不可用”，需要新计算时明确返回本地读取模式。
- **研究偏差**：反复逐日调整直到结果符合预期可能引入选择偏差。最终截止日、回看长度、实际覆盖与计算口径随快照保存；观察调参结果不得直接当作样本外验证。

等待、计算中、来源变化、数据不足、计算失败、尚未验证必须有不同文案。例：“卖压减轻，但仍连续净卖出；盘口恢复尚未验证。”为结构示意，不是某只股票的真实结论。

### 11.2 接入和验收要求

- 日级聚合优先增量复用，不因切换比较窗口反复扫描全部原始逐笔；源数据变化使受影响日期与后续依赖特征失效。
- 连续特征优先由服务端生成并随报告冻结；按需计算时使用服务端结果凭据，绑定股票、日期、观察参数、来源和结果摘要，不直接信任浏览器提交的状态值。
- 快照保存观察窗口、事件规则／起点、结果、质量状态与口径。快照禁止重新计算；旧快照缺少连续状态时显示“保存时未包含”，不从新数据补齐。
- 快照允许切换已经冻结的窗口；其余窗口显示“快照中不可用”。顶层观察设置与质量对象同样需要服务端校验，不能只验证卡片字段。
- 来源身份覆盖真正使用的文件、复权与基准、日历、规则参数和计算代码；新旧根目录不得混用。展示样式变化不应被误称为原始数据变化。
- 最小验收：窗口加权比例正确；三点仅两次改善；缺口不跨越；左截断正确；停牌不补零；未知方向使相关标签降级；20日不足但短窗可用；未来数据不改变此前状态；切换参数无旧结果冒充新结果；历史快照不请求新状态；窄屏可读且原图表／筛选交互不回归。
- 逐日控件验收：周末／休市不计入交易日步进；缺数据的交易日不被跳过；5→6日只改变回看起点，截止日不变；移动截止日保持回看长度并同步整份报告；输入0、负数或非整数被拒绝；单日趋势不伪造；快照未冻结的日期／窗口组合不能计算。
- 实施顺序为日级基础与质量标记、价格／资金／成交连续特征、页面摘要和详情、事件观察、样本外有效性研究。页面和计算通过验收不等于吸筹识别或交易策略已验证。

## 12. 每日记录模板

```text
日期：
股票：
价格：开盘/最高/最低/收盘/涨跌幅/VWAP
成交：成交额/成交量/主动买入/主动卖出/主动净额
父单：>=100万/20-100万/5-20万/<5万净额
盘口：买卖价差/加权深度/买方补单/卖方补单/恢复时间
冲击：实际变化/历史基准偏离
连续状态：当日水平/较前日变化/3日状态/5日状态/阶段状态
决策：候选标签/触发条件/失效条件
验证：后1日/后3日/后5日收益/最大回撤/成交率/滑点
```

## 13. 最终边界

当前 Vue 入口接入来源门槛后的报告、质量证据、市场轨迹、连续观察及日级事件层、全库候选、前复权日 K／当日分钟 K（含成交量）、按需单股深查、独立形态筛选与事件图表，以及仅冻结本页已读取证据的快照保存／只读恢复。图表每次实时悬浮重新读取本地文件；快照缺图、缺深查、缺连续窗口／事件层、缺形态结果或缺形态详情时不补读。完整深查证据及形态结果在保存时与服务端当前来源身份和结果凭据核验，旧快照缺少事件或形态字段时只读显示不可用。旧页保留作迁移对照；日级事件后续收益仅有命令行描述性研究，未纳入页面或可执行策略回测。交易授权和形成中的自动吸筹判断仍未实现。新入口不是新的研究数据源。

个股价格形态、技术指标及K线信号标注的设计与实现边界统一见 `个股形态识别设计.md`：Vue 已接入独立“个股形态”Tab、22类研究规则、组合筛选、图上标注及逐根K线详情，不另建应用，不改变已有候选与连续观察区域。日K及形态从本地 `stk_factor_pro_by_code` 读取qfq字段，不依赖adj_factor，保持截止日截断；原by_date路径继续供已有连续状态使用。形态扫描是按需计算，不等于全量样本回测；复杂形态收益有效性尚未完成。

### 连续观察正文的当前实现

连续观察独立展示市场逐日轨迹、结构变化统计和个股证据卡片，原“候选形成与变化”区域不改动。方向轨迹采用窗口每日均有效的共同股票集合，金额加权净额比为净额合计／成交额合计；成交额按各日独立覆盖，并列出覆盖数。共同样本为空时不可比较，不补零。

结构变化比较窗口内最后两个交易日，逐项列出改善、恶化、持平、未知和严格跨零转向；单日窗口不适用。独立卡片按代码排序，每页20只，可搜索名称／代码及筛选资金变化，展开逐日来源。价格涨跌与资金改善分别描述，小幅变化按原始精度判断。覆盖说明降为正文末尾辅助信息，不输出自动吸筹或买卖确认。

新快照以已验证的状态凭据派生并冻结正文和口径标识，使用与浏览器相同的纯展示模型（保存含状态的快照需要 Node.js）；旧快照不补读新数据。新增正文不重扫原始 Level-2，不替代撤补单和关联订单身份验证。

Level-2 是验证工具，不是意图读取器。任何“吸筹”“派发”“主力推动”判断，都必须回到成交方向、价格冲击、盘口恢复、父单持续性、关键位置和后续价格结果。

研究闭环：

```text
数据质量 → 状态识别 → 候选生成 → 关键位置确认
→ 风险退出 → 后续结果验证
```

## 附录：日期化案例边界

### 页面日期与快照使用规则

本地数据路径统一配置在同目录 `level2_paths.json`：`level2` 为三类正式文件目录，`stock_minute` 为分钟 Parquet 目录，`stk_factor_pro` 为前复权因子按日 CSV 目录，`daily` 为日线按日 CSV 目录，`stock_basic` 为股票名称 CSV 文件。填写绝对路径（空格无需加反斜杠），修改后重启 `level2_detail_server.py` 生效。不移动源文件、不运行中热切换；切换来源后重新计算报告，原快照保持冻结。

- 页面按本地正式文件列出日期。切换日期必须加载该日整份报告，近7交易日窗口与连续状态共用本地 SSE 交易日历；日历缺日/重复或中间缺文件/未提交时禁止计算，不跳过缺失日。
- 尚无报告的日期先点击“计算所选日期”，完成后“查看所选日期”。日K、分钟K和按需深查均绑定报告日，禁止混用其他日期。
- 最新数据模式下，每次悬浮重新读取本地图表数据；本地最新不等于交易所实时行情。
- “保存快照”冻结整份当前报告、已加载详情和图表、筛选排序状态，保存在工作目录的 `level2_snapshots`。未查看图表不会自动全量计算；快照中明确显示未保存。
- 快照只读，不追加最新行情或重新计算。以保存时间和报告日识别，保持统一页面入口，不使用 v1/v2 等报告版本号。
- 快照包含完整性校验及来源信息，但历史日期的事后快照不证明历史当时可得性。旧报告缺少生成时源指纹时明确标识未知；大文件来源以路径、大小和修改时间识别，不冒充原始文件全量哈希验证。

百普赛斯或2026-09-22全市场复盘只能作为日期化案例。案例中的数值、股票名单、支撑位和主动净额用于说明如何应用规则，不能直接复制为下一交易日的阈值。
