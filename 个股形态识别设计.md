# 个股形态识别设计

## 定位与实现状态

本文件记录已讨论的个股形态功能范围，作为持续维护的统一设计文档，不以 v1、v2 或运行批次拆分。目前已编写独立形态Tab、22类研究规则、筛选及图上证据展示；50项Python测试及K线交互测试通过，20260922的5,209只股票已完成by_code全市场计算，真实页面已核对qfq图、成交量、指标及逐根详情。仍不表示形态收益有效性、全部复杂结构与所有组合交互已验证。

目标是在现有 Level-2 页面中并列展示价格形态与资金证据，支持筛选、查看形成过程、确认条件及失效条件。仅用于研究，不构成交易授权。形态成立不等于存在收益优势，看涨形态不等于买点。

本文件负责形态Tab、形态事件、信号标注及技术指标联动；`Level2数据实战使用框架.md` 负责已有市场复盘与连续状态。独立Tab属于同一应用，不是新增应用入口。已有筛选或K线组件可复用，不代表形态识别已实现。

## 形态清单

以下均纳入计划，包括复杂结构，不因主观边界较多而排除。

| 分组 | 形态 | 主要证据 |
|---|---|---|
| 短期K线组合 | 红三兵、三只乌鸦 | 连续实体、收盘递进、开盘位置、上下影线及此前趋势 |
| 短期K线组合 | 早晨之星、黄昏之星 | 前后实体方向、中间K线实体大小、第三根回收幅度及此前趋势 |
| 短期K线组合 | 看涨吞没、看跌吞没 | 实体包覆关系及此前价格位置，不以高低价包覆替代实体定义 |
| 单根K线候选 | 锤头线、流星线 | 实体与影线比例、实体位置、此前趋势；不单凭一根K线确认反转 |
| 底部／顶部结构 | 双底、双顶 | 两个拐点的间隔与深度差、颈线、突破及回撤 |
| 底部／顶部结构 | 头肩底、头肩顶 | 左肩、头部、右肩、颈线、结构比例与确认时间 |
| 底部结构 | 圆弧底 | 多日价格弧度、低位持续时间、恢复过程与成交量 |
| 底部／整理结构 | 杯柄 | 杯深、左右杯沿、恢复过程、柄部回撤与突破；不将任意V形反弹视作杯柄 |
| 趋势与整理 | 平台突破、突破回踩 | 事前平台边界、突破日期、成交量与回踩位置 |
| 趋势与整理 | 上升趋势回调、箱体下沿企稳 | 高低点结构、回调幅度、区间边界与成交变化 |
| 趋势与整理 | 三角收敛、旗形整理 | 高低点边界收敛或整理通道、前置趋势与突破方向 |
| 失败／风险形态 | 突破失败、支撑跌破 | 已识别关键区域、跌回区间或破位日期、反抽表现 |

“经典”表示常见技术分析概念，不表示这些形态已在本地样本中证实有效。

## 识别与状态规则

- 分开识别短期K线组合与多日价格结构，不使用同一套规则套用所有形态。
- 允许一只股票同时具有多个形态，保留相反方向证据，不强制归入单一类别。
- 形态事件阶段采用：**形成中／规则已确认／已失效**。并非每种形态必须经过全部阶段；红三兵等完成型组合不强造“形成中”。规则已确认只指满足定义，不表示走势必然兑现，也不表示永远有效。
- 数据可评估性独立标记：可评估／历史不足／数据异常；任务执行状态独立标记：未实现／未计算／计算中／完成／失败。“未识别”仅用于规则已执行、数据可评估但没有匹配事件，不代替失败或缺数据。
- 同一事件从形成、确认到失效保留身份；失效后出现的新结构生成新事件，不移动旧起点使旧形态永不失败。各形态的有效期及历史展示范围仍须规则表定稿，过期不能默认为价格失效。
- 显示形态起点、识别时点、确认日、失效日；尚未发生的日期留空，不推测补齐。
- 红三兵不能仅以连续三根阳线识别，还需检查实体、收盘递进、开盘关系、影线及此前位置。
- 双底等结构必须定义拐点间隔、深度差、颈线及突破条件；杯柄需独立定义杯部与柄部，不能复用双底标签。
- 对颈线、平台和支撑使用可追溯的价格区间与生成依据，不宣称是真实主力成本。
- 当前研究默认值在 `level2_patterns.py` 的规则与逐事件证据中公开：输入180交易日，展示最近60交易日发生识别／确认／失效的事件，识别后20交易日内作为活动事件；过期不等于失效。拐点使用左右各2根有效K线，右侧完成后才可识别。复杂结构采用可检验的研究近似，不表示统一行业标准，尚未进行收益校准。

## 数据与时间边界

- 价格形态与日K预览直接使用本地 `stk_factor_pro` 的 `open_qfq/high_qfq/low_qfq/close_qfq`；MA、MACD、RSI、KDJ读取对应qfq字段，不依赖 `adj_factor`，不混用hfq或原始价格，不再进行额外复权缩放。沿用来源qfq基准，不宣称已独立校验为报告日基准；跨分区复权基准一致性及供应商平滑参数仍未独立核验。qfq缺失或OHLC异常保持缺口。成交量与成交额保留原始口径。图上支持逐根悬浮详情及左右方向键，展示日期、OHLC、较前交易日变化、量额和技术指标，缺失前日时涨跌幅保持未知。
- 形态历史窗口独立于资金的3／5／20日观察窗口；较长结构需要足够历史，不能用3日数据冒充完整平台或底部结构。
- 所选报告日是整个页面的数据上界；回溯识别每个历史信号时，其判定输入还须截断至该信号判定日。拐点确认日与整个形态确认日分开记录，不能把后续确认回填到拐点日。
- 不跨缺失交易日拼出“连续三根K线”。停牌、缺失、异常OHLC、除权口径异常需分别处理，无法辨别时明确标记未知。
- 保留代码、名称、来源、截止日、有效数据覆盖、规则标识及参数，使结果可复核。
- 后续走势仅用于独立验证，不参与当日形态识别。旧快照不读取新行情补写历史结论。

## 与 Level-2 证据的关系

价格形态与 Level-2 独立计算、并列展示，不互相替代：

| 价格侧 | 可观察的资金侧 | 判断边界 |
|---|---|---|
| 平台突破／杯柄突破 | 主动净额比增强、窗口净买入 | 不等于突破必然有效 |
| 突破回踩／趋势回调 | 净卖出减轻、连续改善 | 不等于已确认被动吸筹 |
| 箱体下沿／双底候选 | 价格抗跌与卖压变化 | 未计算冲击和补单时，不确认盘口承接 |
| 突破失败／支撑跌破 | 主动净额转弱、连续净卖出 | 不据此推断特定机构派发 |

补单、撤单、订单身份与价格冲击未验证的部分，继续显示未计算／未验证，不因加入形态标签而升级为结论。

## 页面展示与筛选

在本工程新增独立“个股形态”Tab，默认采用紧凑列表＋详情抽屉，不继续扩张连续观察卡片；原“候选形成与变化”及连续观察区域保持不变。同股一行，多形态并列，点击具体形态进入详情；窄屏详情使用全屏抽屉。代码悬浮保留简版K线预览。

- 两组标签：**价格形态**与**Level-2证据**，清楚区分方向、阶段和覆盖。
- 基础筛选包含代码／名称、形态家族及具体形态、事件阶段、事件时效。默认阶段为形成中＋规则已确认；默认不限方向、成交额与时效，不隐含看涨推荐。默认每页20只，按首次识别日倒序、代码升序，未知排序值置后。
- 多形态默认任一满足（OR），高级条件可选全部共存（AND）；不同筛选组之间AND。同一形态的类型、阶段、时间及结构条件必须由同一事件满足；全部共存要求每个所选形态各有至少一条完整匹配事件。结果按股票去重，同时展示股票数与事件数。
- 事件时效明确选择首次识别／确认／失效日期，距离按交易日计；不能用形态起点代替信号发生日。“旧双底已失效＋新吞没已确认”不得命中“双底＋已确认”。
- 高级筛选分为结构、量价、技术指标、Level-2及覆盖。资金窗口独立注明，默认不启用资金条件；缺Level-2不隐藏可识别的价格形态。未知值不满足已启用的数值条件，不作为零；不适用的关键位距离单独标识。
- 结构筛选的距离须标明颈线、突破位或失效位及计算方式；成交量相对事前均量与成交额变化分开。阈值默认留空，具体形态阈值不由通用筛选反向决定。
- 展开显示：识别依据、关键区域、确认条件、失效条件、形成日期及逐日证据。
- 代码悬浮复用现有简版前复权日K与成交量组件，不叠加完整形态线与技术副图。点击形态进入详情后，才展示该事件的完整标注及可选指标，避免浮层膨胀与多形态遮挡。
- 数据不足、不符合任何规则和数据异常分别展示，不用空白代替状态。
- 新快照冻结已计算形态事件、规则参数、指标证据及标注坐标；仅冻结已查看图表的数据，不自动加载全部股票K线。标注坐标已保存不等于图表数据已保存；未保存的图表明确不可用。旧快照不补算新功能、不补读最新数据。

示例仅用于说明展示结构，不代表某只股票实际满足条件：

> 价格形态：双底 · 形成中，尚未突破颈线。
> 资金证据：连续净卖出，但最近一日卖压减轻。
> 展开查看两个低点、颈线、日期、确认与失效规则。

## K线信号标注与技术指标联动

本节为持续维护的产品设计。已接入本地qfq MA／MACD／RSI6／KDJ与图上事件标注，字段存在性已核验；供应商指标算法与收益有效性尚未独立验证。用户提及的 `skt_factor_pro` 按本工程数据源名称理解为 `stk_factor_pro`，不新增另一套同名数据源。

### 三层展示

| 层次 | 展示内容 | 默认行为 |
|---|---|---|
| K线主图 | 形态区间、关键拐点、颈线／平台、信号标记，可选均线叠加 | 仅标注当前选中形态，允许关闭标注 |
| 副图 | 原始成交量；MACD、RSI、KDJ等可选指标 | 保留成交量，技术指标默认一次只展示一个；仅提供已核验字段 |
| 信号详情 | 触发日期、规则、当时指标数值、独立Level-2证据、失效条件 | 悬浮信号快速查看，点击进入完整证据 |

代码悬浮显示简版前复权K线与成交量，尺寸沿用现有浮层；完整标注及技术指标副图放在形态详情抽屉，不将所有信息塞进悬浮窗。

### 信号标记及时间语义

- **○ 形成中**：已满足该形态的可辨认结构规则，但确认条件未成立。不得把任意两根阳线自动视为红三兵形成中。
- **▲ 规则确认**：标在确认信息实际出现的交易日，而非形态起始日或事后找到的最低点。符号表示规则确认，不表示买入；看跌形态必须同时显示方向文字。
- **× 已失效**：标在失效条件首次成立的交易日；保留先前确认事件，不抹去失败记录。
- 三类标记均带形态名称、方向与阶段说明，不只依赖颜色区分；同日多事件可展开选择，避免箭头重叠。
- 主图区间可以回指历史结构，但信号可见时间不能回填。形态起始、首次识别、确认和失效日期分别保存。

示例浮层仅为展示模板，不代表实际信号：

> 双底 · 规则确认：当日收盘满足颈线突破条件。
> 趋势指标：具体条件及满足／不满足／未知。
> 动量指标：当日数值、比较基准与变化。
> 成交量：相对事前均量的倍数及基准窗口。
> Level-2：独立资金证据，不替代价格确认。
> 失效：规则定义的价格位置与触发方式。

### 技术指标用途

| 用途 | 候选指标／条件 | 解释边界 |
|---|---|---|
| 趋势背景 | 均线方向、价格相对均线的位置 | 是背景条件，不自动确认形态有效 |
| 动量辅助 | MACD、RSI、KDJ的水平、变化、交叉或背离 | 参数、比较窗口及背离拐点规则须独立定义，不能仅凭图形目测 |
| 波动尺度 | ATR等波动指标（仅在字段存在且核验后） | 用于表达突破／失效距离；不直接生成统一止损建议 |

指标作为可选辅助条件，不默认要求全部指标同时通过。价格形态、指标条件和Level-2证据分栏展示；多个相关指标不视作相互独立的确认，不合成为未经校准的成功概率。

### 字段与价格尺度核验

实施前建立本地字段映射：字段名、指标含义、周期／参数、单位、原始或复权口径、可用起止日、预热需求、缺失与异常比例。未核验字段不在页面宣称可用，缺失不得填零。

- 形态价格、均线、颈线和其他价格量纲指标必须与主图使用同一复权基准。不得直接把不匹配的原始均线叠加到前复权K线上。
- MACD等指标的单位与复权处理亦须核验；不可因为是副图就忽略价格尺度。RSI／KDJ等无量纲指标仍需确认计算定义和周期。
- 优先读取已核验的本地指标。若需重新计算缺失指标，须明确算法与参数、输入价格口径和预热覆盖，标注为本地重算，不冒充数据源原始字段。
- 每个信号仅使用截至其判定日的信息。仅按报告截止日截断并不能消除历史文件修订带来的偏差，不宣称严格实盘时点可得性已验证。
- 原始成交量不随价格复权；量能倍数与成交额变化分开，不互相替代。

### 与现有图表和快照的衔接

现有日K预览默认约60日，不能作为所有复杂形态的固定历史范围。形态识别使用完整交易日历和有效性标记，缺失／异常K线不能被删除后拼接成连续形态；详情图须覆盖完整事件及必要前置背景。

活页代码悬浮仍读取截至报告日的本地最新文件，仅作价格预览。详情中的标注图、指标与形态事件须核验来源一致；来源改变时提示“行情已更新，形态待重算”，不叠加过期颈线或使用旧指标解释新K线。价格预览更新不等于形态结果刷新。快照遵循上述冻结范围，并通过服务端凭据核验，不信任前端自行提交的形态标签。

### 专项验收

- 信号标记与实际确认／失效日期一致；新增未来K线不能改变已冻结的历史信号。
- 除权前后，主图、均线、关键位和价格量纲指标尺度一致。
- 指标预热不足、缺交易日、异常OHLC、来源变更分别显示正确状态。
- 多形态同日标记可选择，代码悬浮、详情切换、键盘访问、窄屏和快照恢复可用。
- 图上显示的指标值与本地已核验字段或明确标注的重算结果逐项对账。

## 验收与研究验证

1. 每种形态提供正例、反例、阈值边界、数据不足及异常数据测试；明确适用条件，不追求所有股票都有标签。
2. 在相同原始数据修订和规则参数下，验证历史逐日截断与回放结果一致；加入未来行不改变较早判定结果。后来修订原始数据可产生新的回溯结果，但不得覆盖已冻结快照，且须标明来源变化。
3. 验证复权口径、停牌／缺失交易日处理及形态确认延迟。
4. 页面核对标签与K线标注一致，筛选、窗口切换、分页、窄屏及快照恢复正常。
5. 在独立历史样本评估形态发生频率、后续表现、失败率、回撤与样本流失；区分单纯形态与叠加 Level-2 的增量价值。
6. 记录参数试验与多重检验风险，纳入交易成本和可执行性；未经验证不展示成功率、胜率或自动买卖建议。

## 下一步待定事项

产品层面已确定独立Tab、紧凑列表＋详情抽屉、多形态筛选语义、简版悬浮与完整标注图的分工，以及快照边界。

当前接入：日K与形态输入固定读取 `level2_paths.json` 中 `stk_factor_pro_by_code`，目前为 `/Users/m4pro/Documents/claude/quant-platform/data/sync/stock/special/stk_factor_pro/by_code`，文件名例如 `000977_SZ_stk_factor_pro.csv`。不回退by_date，先按报告截止日／交易日窗口截断再识别。日K每次请求重读对应个股文件；形态结果按代码、参数与来源指纹校验。原 `stk_factor_pro` 的by_date配置保留给既有日级连续状态，不混入新图表与形态输入。

仍待完善：复杂形态完整正反样本回归、参数敏感性与独立历史验证、复权跨期一致性独立核验、标注碰撞处理，以及全部组合交互验收。页面完成不能替代算法和数据验收。
